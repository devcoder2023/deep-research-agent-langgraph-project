[project]
name = "ai-agents-starter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.4",
    "langgraph>=1.2.11",
    "langchain-openai>=1.2",
    "python-dotenv>=1.0",
    "beautifulsoup4>=4.12",
    "requests>=2.31",
    "notebook>=7.6.2",
]

[dependency-groups]
dev = ["jupyterlab>=4"]
